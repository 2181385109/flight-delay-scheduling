# 使用说明书（航班延误预测与智能调度优化系统）

本手册面向使用者，手把手讲清楚**怎么装、怎么跑、怎么看结果、怎么改**。
命令均以 **Windows PowerShell** 为例（macOS/Linux 见每节的备注）。

> 项目根目录：`D:\xiangmu\flight-delay-scheduling`
> 一句话上手：进入该目录，运行 `python -m flightopt run-all`，等约 40 秒即可出全部结果。

---

## 目录
1. [这个项目是做什么的](#1-这个项目是做什么的)
2. [环境要求](#2-环境要求)
3. [安装](#3-安装)
4. [一键运行（最常用）](#4-一键运行最常用)
5. [分步命令详解](#5-分步命令详解)
6. [产物在哪、都是什么](#6-产物在哪都是什么)
7. [交互式网页面板（Streamlit）](#7-交互式网页面板streamlit)
8. [配置文件 config.yaml 详解](#8-配置文件-configyaml-详解)
9. [用真实数据替换合成数据](#9-用真实数据替换合成数据)
10. [运行测试](#10-运行测试)
11. [发布到 GitHub（剩余步骤）](#11-发布到-github剩余步骤)
12. [常见问题排查](#12-常见问题排查)

---

## 1. 这个项目是做什么的

一个 **“预测 → 分级 → 调度” 的闭环系统**：

1. **预测**：用 LightGBM 预测每个航班的起飞延误（分钟）和“延误>15 分钟”的概率。
2. **分级**：把预测结果按分位数分成 5 个风险等级，L4/L5 为高风险。
3. **调度**：用 OR-Tools CP-SAT 约束优化，微调航班起飞时隙，在满足周转/宵禁/跑道容量等约束下，最小化加权总延误。
4. **评估 + 可视化**：复现核心指标、出图、提供交互面板。

数据默认由**内置合成生成器**产生（注入了因果结构），**零外部依赖**即可端到端跑通。

---

## 2. 环境要求

- **Python 3.11+**（本机为 3.12，已验证可用）
- 依赖库：pandas、numpy、scikit-learn、lightgbm、optuna、shap、ortools、matplotlib、plotly、streamlit、pyyaml、typer、pyarrow（安装时自动装好）

> **你的电脑已经装好了全部依赖并完成了项目安装**，可以直接跳到[第 4 节](#4-一键运行最常用)运行。
> 下面第 3 节是“从零开始 / 换台电脑 / 分享给别人”时的标准安装步骤。

---

## 3. 安装

### 3.1 标准安装（推荐，隔离虚拟环境）

```powershell
cd D:\xiangmu\flight-delay-scheduling
python -m venv .venv
.\.venv\Scripts\Activate.ps1          # 激活虚拟环境（每次新开终端都要激活）
pip install -e .                       # 安装本项目及全部依赖
```

- macOS/Linux 激活命令：`source .venv/bin/activate`
- 若 PowerShell 报“无法加载脚本”，先执行：
  `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`

### 3.2 装开发/测试工具（可选）

```powershell
pip install -e ".[dev]"                # 额外装 pytest、ruff
```

安装完成后，验证：

```powershell
python -c "import flightopt; print(flightopt.__version__)"    # 应打印 0.1.0
```

---

## 4. 一键运行（最常用）

```powershell
cd D:\xiangmu\flight-delay-scheduling
python -m flightopt run-all
```

它会依次执行：**生成数据 → 训练 → 分级 → 调度 → 评估 → 出图**，
最后在终端打印四大核心指标表，并把结果写入 `outputs/`。约 40 秒完成。

常用参数：

```powershell
python -m flightopt run-all --trials 50          # 调参试验次数（默认 30，越大越久越准）
python -m flightopt run-all --seed 123           # 换随机种子
python -m flightopt run-all --solver greedy      # 用贪心调度器（默认 cpsat）
```

运行完，打开这几个文件就能看到成果：
- 指标报告：`outputs/reports/report.md`
- 图（含甘特动画）：`outputs/figures/`
- 首页指标表：`README.md`（会被自动填好）

---

## 5. 分步命令详解

如果你想一步步跑、或只重跑某一环，用下面的子命令（顺序执行）：

| 步骤 | 命令 | 作用 | 主要产物 |
|---|---|---|---|
| 1 | `python -m flightopt gen-data` | 生成 ~2000 条合成航班 | `data/processed/flights.parquet` |
| 2 | `python -m flightopt train` | 训练 LightGBM 双头 + 基线 + 调参 | `outputs/models/bundle.pkl`、`predictions.parquet` |
| 3 | `python -m flightopt grade` | 分位数 5 级风险分级 | `data/processed/graded.parquet` |
| 4 | `python -m flightopt schedule --solver cpsat` | 调度优化（`cpsat` 或 `greedy`） | `data/processed/schedule.parquet` |
| 5 | `python -m flightopt evaluate` | 汇总指标、写报告、回填 README | `metrics.json`、`report.md` |
| 6 | `python -m flightopt viz` | 出静态图 + SHAP + 甘特动画 | `outputs/figures/*` |

每个命令都支持 `--config 自定义路径.yaml` 和 `--seed N`。
`train` 支持 `--trials N`；`schedule` 支持 `--solver cpsat|greedy`。

> 说明：分步命令之间通过磁盘文件解耦，所以必须**按顺序**跑（后一步依赖前一步的产物）。想省事就直接用 `run-all`。

---

## 6. 产物在哪、都是什么

```
outputs/
├── models/
│   └── bundle.pkl               # 训练好的模型包（LightGBM 回归+分类、RF 基线、特征统计量）
├── figures/                     # 图
│   ├── delay_distribution.png   #   延误分布直方图
│   ├── feature_importance.png   #   特征重要性（增益）
│   ├── shap_summary.png         #   SHAP 特征影响力（讲清驱动因素）
│   ├── classification_eval.png  #   混淆矩阵 + PR 曲线
│   ├── risk_levels.png          #   各风险等级样本量 & 实际延误率
│   ├── schedule_before_after.png#   高风险航班优化前后延误对比
│   ├── schedule_convergence.png #   优化收敛曲线（总延误 + 约束满足率）
│   ├── gantt.gif                #   甘特动画：航班时隙平移（动图）
│   ├── gantt_final.png          #   甘特最终帧（静态图）
│   └── gantt_interactive.html   #   可交互甘特图（浏览器打开）
└── reports/
    ├── report.md                # ★人类可读的完整结果报告（先看这个）
    ├── metrics.json             #   机器可读的全部指标
    ├── feature_dict.md          #   特征字典（每个特征的定义与动机）
    ├── predict_metrics.json     #   预测环详细指标
    ├── risk_metrics.json        #   分级环详细指标
    └── schedule_metrics.json    #   调度环详细指标（含 CP-SAT vs 贪心对比）

data/processed/                  # 中间数据（parquet，已被 .gitignore 忽略，可随时重生成）
├── flights.parquet             #   合成航班原始数据
├── predictions.parquet         #   逐航班预测（out-of-fold）
├── graded.parquet              #   带风险等级的结果
└── schedule.parquet            #   优化后的时刻表（含每航班时隙偏移）
```

**四大核心指标**（`report.md` / 终端 / README 都能看到）：

| 指标 | 当前结果 | 目标 | 对照基线 |
|---|---|---|---|
| 高风险捕获率（Recall） | ~0.83 | ≥ 0.80 | 规则基线 ~0.43 |
| 调度约束满足率 | ~0.998 | ≥ 0.85 | 贪心 ~0.997 |
| 高风险航班延误下降 | ~3.0 分钟/架次 | ≥ 1.0 | 优化前 |
| 预测误差 MAE | ~3.74 分钟 | 越低越好 | RF 3.98 / 均值 6.47 |

---

## 7. 交互式网页面板（Streamlit）

```powershell
cd D:\xiangmu\flight-delay-scheduling
streamlit run app/streamlit_app.py
```

浏览器会自动打开（默认 http://localhost:8501 ）。面板功能：

- **左侧栏**：拖动滑块设定“调参试验次数、跑道容量、天气强度倍数”，选择调度器，点 **▶ Run full pipeline** 一键重跑。
- **主区**：四个核心指标卡片、风险等级分布、收敛曲线、特征重要性、SHAP 图、甘特动画、以及“被挪动的航班”明细表。

> 想停止：回到终端按 `Ctrl + C`。

---

## 8. 配置文件 config.yaml 详解

所有可调参数都集中在 `config.yaml`，代码里不写死任何魔数。改完直接重跑即可。常调的几处：

```yaml
seed: 42                      # 全局随机种子（保证可复现）

synth:                        # 合成数据
  n_flights: 2000             #   航班数量（越多越慢；改小可加速试验）
  coeffs:                     #   延误因果模型系数（决定各因素影响强度、正类比例）
    a1: 21.0                  #     天气影响
    a2: 15.0                  #     拥堵影响
    a5: 0.70                  #     延误传播强度
    sigma: 4.5                #     噪声（越小信号越干净、越可预测）

predict:
  optuna_trials: 30           #   调参试验次数（也可用 --trials 覆盖）
  test_size: 0.20             #   时间序切分的测试集比例

risk:
  source: delay               #   分级依据："delay"(预测延误) 或 "proba"(延误概率)
  quantiles: [0.2,0.4,0.6,0.8]#   5 级切点
  high_risk_levels: [4, 5]    #   哪些等级算“高风险”

schedule:
  max_offset_min: 30          #   时隙可偏移的最大分钟数(±K)
  runway_capacity: 5          #   每机场每 15 分钟窗口的跑道容量 C
  only_high_risk: false       #   false=优化全部航班(CP-SAT 优势更明显)；true=只优化高风险(更快)
  solver_time_limit_s: 25     #   CP-SAT 求解时限（秒）
  weight_high_risk: 5.0       #   高风险航班在目标函数里的权重
```

改例子：
- 想**更快出结果**：把 `synth.n_flights` 调小（如 800）、`predict.optuna_trials` 调小（如 10）。
- 想**让跑道更紧张、调度更有挑战**：把 `schedule.runway_capacity` 调小（如 3）。
- 想**只优化高风险航班、求解更快**：`schedule.only_high_risk: true`。

也可以复制一份配置另存，用 `--config 你的.yaml` 指定。

---

## 9. 用真实数据替换合成数据

### 9.0 一键跑真实数据基准（推荐先看这个）

项目已内置**真实航班数据基准测试**（nycflights13：2013 年 EWR/JFK/LGA 真实起飞
数据，来自美国 DOT/BTS，含真实逐小时气象）：

```powershell
pip install nycflights13                              # 首次需要，约 10MB
python scripts/benchmark_real_data.py                 # 默认跑 2013 年 1 月，约 1 分钟
python scripts/benchmark_real_data.py --months 1 2 3 --trials 30   # 想跑更多月份
```

产物：
- 真实源数据导出到 `data/raw/nycflights13/*.csv`（约 35MB，不进 Git）
- 中间产物在 `benchmarks/real/`（不进 Git）
- **诚实对比报告** → `outputs/reports/real_data_benchmark.md`

> ⚠️ **重要**：合成数据上的漂亮指标（MAE 3.74）是因为标签由我写的公式生成；
> 真实数据上 MAE 是 **20.85**，捕获率 **0.736（达不到 0.80 目标）**。
> 详见该报告和 README 的”Read this before trusting those numbers”章节。

### 9.1 用你自己的 CSV

1. 准备一份公开航班 CSV（如 Kaggle “Flight Delays” 或美国 BTS 准点数据）。
2. 放到 `data/raw/` 目录下（该目录内容不会被提交到 Git）。
3. 用适配器把它转成统一 Schema 再跑：

```python
from flightopt.config import load_config
from flightopt.data.loader import load_or_default
from flightopt import predict, risk, schedule, evaluate, viz

cfg = load_config()
df = load_or_default(cfg)        # data/raw 有 CSV 就用真实数据，否则用合成数据
predict.run_training(cfg, df)
risk.run_grading(cfg)
schedule.run_scheduling(cfg, solver="cpsat")
evaluate.run_evaluate(cfg)
viz.run_viz(cfg)
```

适配器 `loader.py` 会自动映射常见列名、按机尾+时间近似串接航段、缺失的天气字段用良性默认值填充。之后整条流水线**无需改动**即可复跑。

---

## 10. 运行测试

```powershell
cd D:\xiangmu\flight-delay-scheduling
pytest                         # 或 python -m pytest
```

会跑 28 个用例（数据/特征/预测/分级/调度/评估各一组），用的是缩小的快配置，几十秒内跑完，全绿表示流水线健康。

代码风格检查（可选）：

```powershell
ruff check .
```

---

## 11. 发布到 GitHub（剩余步骤）

项目已在本地初始化 Git 并提交（分支 `main`）。GitHub CLI 已装好，**只差你登录这一步**：

1. 新开一个终端窗口，运行：
   ```powershell
   gh auth login
   ```
2. 依次选择：**GitHub.com → HTTPS → Authenticate Git: Yes → Login with a web browser**，
   复制一次性验证码，在浏览器授权。
3. 看到 `✓ Logged in as <你的用户名>` 即成功。之后可让我继续（我会创建公开仓库并推送），
   或你自己执行：
   ```powershell
   cd D:\xiangmu\flight-delay-scheduling
   gh repo create flight-delay-scheduling --public --source . --remote origin --push
   ```

> 注意：`.gitignore` 已确保**不会提交**大模型文件、数据 parquet、大动画等；源码、配置、报告、小图会被提交。

---

## 12. 常见问题排查

| 现象 | 解决办法 |
|---|---|
| `python` 不是 3.11+ | 装 Python 3.11/3.12，或用 `py -3.12 -m ...` 指定版本 |
| `flightopt` 找不到模块 | 确认已在项目根目录 `pip install -e .`；虚拟环境是否已激活 |
| PowerShell 无法激活 venv | 先 `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |
| 运行太慢 | 调小 `config.yaml` 的 `n_flights`、`optuna_trials`；或 `--trials 10` |
| SHAP/甘特图没生成 | 属于“尽力而为”步骤，失败会打印提示并跳过，不影响主流程；重跑 `viz` 即可 |
| CP-SAT 求解久 | 调小 `solver_time_limit_s`，或设 `only_high_risk: true` |
| 想彻底重来 | 删除 `data/processed/` 与 `outputs/`（除 .gitkeep），重跑 `run-all` |

---

有任何一步卡住，把终端报错贴出来即可。祝使用顺利 ✈️
