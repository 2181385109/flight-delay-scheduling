# 使用说明书（航班延误预测与智能调度优化系统 · 真实数据版）

本手册面向使用者，手把手讲清楚**怎么装、怎么跑、怎么看结果、怎么改**。
命令均以 **Windows PowerShell** 为例（macOS/Linux 见每节备注）。

> 项目根目录：`D:\xiangmu\flight-delay-scheduling`
> 一句话上手：进入该目录，运行 `python -m flightopt run-all`。

---

## 目录
1. [这个项目是做什么的](#1-这个项目是做什么的)
2. [数据从哪来](#2-数据从哪来)
3. [环境要求与安装](#3-环境要求与安装)
4. [一键运行](#4-一键运行)
5. [分步命令详解](#5-分步命令详解)
6. [产物在哪、都是什么](#6-产物在哪都是什么)
7. [交互式网页面板](#7-交互式网页面板)
8. [配置文件 config.yaml 详解](#8-配置文件-configyaml-详解)
9. [换成你自己的真实数据](#9-换成你自己的真实数据)
10. [运行测试](#10-运行测试)
11. [★ 如何正确看待这些指标](#11--如何正确看待这些指标)
12. [常见问题排查](#12-常见问题排查)

---

## 1. 这个项目是做什么的

一个基于**真实航班数据**的 **“预测 → 分级 → 调度” 闭环系统**：

1. **预测**：用 LightGBM 预测每个航班的起飞延误（分钟）与“延误>15 分钟”的概率。
2. **分级**：按分位数把预测结果分成 5 个风险等级，L4/L5 为高风险。
3. **调度**：用 OR-Tools CP-SAT 约束优化微调起飞时隙，在满足**周转、宵禁、跑道容量**
   等真实运行约束下，最小化加权总延误。
4. **评估 + 可视化**：出指标报告、静态图、甘特动画、交互面板。

---

## 2. 数据从哪来

**真实数据，不是模拟的**：

| 项目 | 内容 |
|---|---|
| 数据集 | **nycflights13** |
| 来源 | 美国交通部 **DOT/BTS** 准点数据库 |
| 范围 | 2013 年 **EWR / JFK / LGA**（纽约三大机场）**全部起飞航班** |
| 规模 | 全年 33.6 万条；默认使用 **1–3 月约 8 万条** |
| 含真实 | 机尾号（延误传播）、**逐小时机场气象**（能见度/风速/降水/温度/湿度/气压）、机型 |

数据以 **pip 依赖**形式随项目安装（`nycflights13`，约 10MB），**无需手动下载、无需 API key**，装好后全程离线可跑。

> 想用自己的 CSV？见[第 9 节](#9-换成你自己的真实数据)。

---

## 3. 环境要求与安装

- **Python 3.11+**（本机 3.12，已验证）

```powershell
cd D:\xiangmu\flight-delay-scheduling
python -m venv .venv
.\.venv\Scripts\Activate.ps1          # macOS/Linux: source .venv/bin/activate
pip install -e .                       # 安装项目 + 全部依赖（含真实数据集）
```

- 若 PowerShell 报“无法加载脚本”：先执行
  `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`
- 装测试工具（可选）：`pip install -e ".[dev]"`

验证：`python -c "import flightopt; print(flightopt.__version__)"` → 应输出 `0.1.0`

---

## 4. 一键运行

```powershell
cd D:\xiangmu\flight-delay-scheduling
python -m flightopt run-all
```

依次执行：**加载真实数据 → 训练 → 分级 → 调度 → 评估 → 出图**，
终端会打印核心指标表，结果写入 `outputs/`。默认 3 个月数据约需数分钟。

常用参数：

```powershell
python -m flightopt run-all --trials 40        # 调参试验次数（默认15，越大越久越准）
python -m flightopt run-all --seed 123         # 换随机种子
python -m flightopt run-all --solver greedy    # 用贪心调度器（默认 cpsat）
```

跑完看三处成果：
- **完整报告** → `outputs/reports/report.md`
- **所有图（含甘特动画）** → `outputs/figures/`
- **首页指标表** → `README.md`（自动回填）

---

## 5. 分步命令详解

| 步骤 | 命令 | 作用 |
|---|---|---|
| 1 | `python -m flightopt fetch-data` | 加载真实航班数据 → `data/processed/flights.parquet` |
| 2 | `python -m flightopt train` | 训练 LightGBM 双头 + 基线 + Optuna 调参 |
| 3 | `python -m flightopt grade` | 分位数 5 级风险分级 |
| 4 | `python -m flightopt schedule --solver cpsat` | 调度优化（`cpsat` 或 `greedy`） |
| 5 | `python -m flightopt evaluate` | 汇总指标、写报告、回填 README |
| 6 | `python -m flightopt viz` | 出静态图 + SHAP + 甘特动画 |

> 各步骤通过磁盘文件解耦，必须**按顺序**执行；嫌麻烦直接用 `run-all`。
> 改了 `config.yaml` 里的 `data.months` / `data.year` 后，要重跑 `fetch-data`（它会强制刷新缓存）。

---

## 6. 产物在哪、都是什么

```
outputs/
├── models/bundle.pkl            # 训练好的模型包（LightGBM 回归+分类、RF基线、特征统计量）
├── figures/
│   ├── delay_distribution.png   # 真实延误分布
│   ├── feature_importance.png   # 特征重要性
│   ├── shap_summary.png         # SHAP：模型到底在用哪些真实因素
│   ├── classification_eval.png  # 混淆矩阵 + PR 曲线
│   ├── risk_levels.png          # 各风险等级样本量 & 实际延误率
│   ├── schedule_before_after.png# 高风险航班优化前后对比
│   ├── schedule_convergence.png # 优化收敛曲线
│   ├── gantt.gif / gantt_final.png / gantt_interactive.html
└── reports/
    ├── report.md                # ★人类可读完整报告（先看这个）
    ├── metrics.json             # 机器可读全部指标
    └── feature_dict.md          # 特征字典（按“防泄露策略”分组）

data/processed/                  # 中间产物（parquet，不进 Git，可随时重生成）
```

---

## 7. 交互式网页面板

```powershell
streamlit run app/streamlit_app.py
```

左侧可调：**使用哪几个月、预测提前量（分钟）、调参次数、跑道容量、调度器**，
点 **▶ Run full pipeline** 一键重跑；主区看指标卡片、风险分布、收敛曲线、
SHAP、甘特动画和“被挪动的航班”明细。停止按 `Ctrl+C`。

---

## 8. 配置文件 config.yaml 详解

所有可调参数集中在 `config.yaml`，代码里不写死任何魔数。

```yaml
seed: 42

data:
  source: nycflights13
  year: 2013
  months: [1, 2, 3]             # 用哪几个月（越多越慢、指标越稳）
  prediction_horizon_min: 60    # ★最重要：预测提前量。网络状态特征只能用
                                #   起飞前 60 分钟之前的信息。调小=题目变简单
  network_window_min: 180       # 网络状态回看窗口
  default_min_turnaround: 30    # 数据里没有，假定的最小过站时间

predict:
  optuna_trials: 15             # 调参次数（也可用 --trials 覆盖）
  group_kfold_splits: 3
  rf_n_estimators: 150          # RF 基线树数
  test_size: 0.20               # 时序切分测试集比例

risk:
  source: delay                 # 分级依据："delay"(预测延误) 或 "proba"(延误概率)
  high_risk_levels: [4, 5]      # 哪些等级算高风险

schedule:
  max_offset_min: 30            # 时隙最大偏移 ±K 分钟
  runway_capacity: 8            # 每机场每 15 分钟窗口容量 C
  only_high_risk: false         # false=优化全部航班；true=只优化高风险（更快）
  solver_time_limit_s: 25       # CP-SAT 求解时限
```

**常见改法**：
- 想**跑得更快**：`data.months: [1]`、`predict.optuna_trials: 5`
- 想**指标更好**：`data.months: [1,2,3,4,5,6]`、`--trials 40`
- 想**让题目更难/更真实**：把 `prediction_horizon_min` 调大（如 120）
- 想**跑道更紧张**：`schedule.runway_capacity` 调小

---

## 9. 换成你自己的真实数据

把任意公开航班 CSV（如 BTS 导出、Kaggle "Flight Delays"）放进 `data/raw/`，
然后 `python -m flightopt fetch-data`。

`loader.py` 的通用适配器会自动识别常见列名（`FL_DATE`、`OP_CARRIER`、
`TAIL_NUM`、`DEP_DELAY`、`CRS_DEP_TIME` 等），映射到统一 Schema，
按机尾+时间重建航段顺序与延误传播。

> ⚠️ 注意：通用 CSV 通常**不含气象**，此时气象特征会被填成良性默认值（等于失效），
> 指标会相应下降——这是如实反映，不是 bug。

---

## 10. 运行测试

```powershell
pytest                    # 30 个用例，跑真实数据的 10 天切片，约 1 分钟
ruff check .              # 代码规范
```

测试里包含几个**专门验证防数据泄露**的用例，比如：
- 篡改“未来”航班的延误值，**不得**改变任何更早航班的特征
- 航班自身的延误**绝不能**进入它自己的网络状态特征
- 测试集标签翻转后，目标编码特征**必须纹丝不动**

---

## 11. ★ 如何正确看待这些指标

这一节很重要，尤其是拿去面试或展示的时候。

**真实航班延误非常难预测**，本项目如实呈现，不粉饰：

1. **误差是"几十分钟"量级，不是个位数。** 延误的主因（空管流控、机组/接续、
   上游航班网络状态、机械故障）**根本不在这份数据里**，学不到就是学不到。
   任何号称能把真实延误预测到几分钟误差的模型，都值得怀疑。

2. **最强的特征是"网络状态"，但它带一个前提。** `airport_delay_state`
   （机场当前已经堵成什么样）是最有效的真实信号，但它**严格滞后**
   `prediction_horizon_min`（默认 60 分钟）。这等于在回答：
   *"已知一小时前为止发生的一切，接下来会怎样？"*
   把这个值调小，指标会变好看——但那是在解一道更简单的题。所以这个假设被显式写在配置里。

3. **调度那个"延误下降"是"预测延误"的下降**，不是真实延误的下降。
   要证明真实下降需要反事实实验，观测数据做不到。

4. **只有"约束满足率"是确定性的**——周转/宵禁/容量是可行性数学，和预测准不准无关。

5. **基线是认真选的，不是稻草人**：回归基线包含**中位数预测**
   （在重尾分布下，中位数是 MAE 意义下最优的常数预测），
   分类基线是一条合理的调度员经验规则。

---

## 12. 常见问题排查

| 现象 | 解决办法 |
|---|---|
| `ModuleNotFoundError: nycflights13` | 重新执行 `pip install -e .` |
| `flightopt` 找不到模块 | 确认在项目根目录 `pip install -e .`，虚拟环境已激活 |
| PowerShell 无法激活 venv | `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |
| 运行太慢 | `data.months: [1]`、`--trials 5` |
| 改了 months 但数据没变 | 跑 `python -m flightopt fetch-data`（强制刷新缓存） |
| CP-SAT 求解久 | 调小 `solver_time_limit_s`，或设 `only_high_risk: true` |
| SHAP/甘特图没生成 | 属"尽力而为"步骤，失败会打印提示并跳过，重跑 `viz` 即可 |
| 想彻底重来 | 删除 `data/processed/` 与 `outputs/` 里的产物，重跑 `run-all` |

---

有任何一步卡住，把终端报错贴出来即可。祝使用顺利 ✈️
